server:
  port: 9101

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 这个是消费者需要访问的微服务名称
service-url:
  nacos-user-service: http://nacos-payment-provider
